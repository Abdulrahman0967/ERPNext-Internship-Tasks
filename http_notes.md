# ملخص أساسيات الويب: HTTP و REST API و JSON

### 1. دورة الطلب والاستجابة (Request / Response Cycle)
تبدأ العملية من العميل (المتصفح أو التطبيق) بإرسال طلب (HTTP Request) إلى الخادم، يحتوي على:
* عنوان المسار (URL / Endpoint).
* نوع العملية (HTTP Method).
* الترويسات (Headers) التي تحدد نوع البيانات وغيرها.
* جسم الطلب (Body/Payload) إن وجد.

يقوم الخادم بمعالجة الطلب وإرجاع استجابة (HTTP Response) تحتوي على:
* رمز الحالة (Status Code).
* بيانات النتيجة (Response Body)، وغالباً ما تكون بصيغة JSON.

---

### 2. طرق الإرسال الأساسية (HTTP Methods)
* **GET:** تستخدم لجلب واستعراض البيانات من الخادم دون تعديلها.
* **POST:** تستخدم لإرسال بيانات جديدة إلى الخادم لإنشاء عنصر أو معالجة عملية.
* **PUT / PATCH:** تستخدم لتعديل وتحديث بيانات موجودة مسبقاً.
* **DELETE:** تستخدم لحذف مورد من الخادم.

---

### 3. أشهر رموز الحالة (HTTP Status Codes)
* **200 OK:** نجحت العملية وتم إرجاع البيانات المطلوبة.
* **201 Created:** نجحت عملية الإنشاء (غالباً بعد طلب POST).
* **400 Bad Request:** خطأ في صياغة الطلب أو البيانات المرسلة من العميل.
* **401 Unauthorized:** العميل غير مسجل الدخول أو يفتقد لبيانات التحقق.
* **404 Not Found:** الرابط أو المورد المطلوب غير موجود في الخادم.
* **500 Internal Server Error:** حدث خطأ برمجي غير متوقع داخل الخادم.

---

### 4. ماهية REST API وصيغة JSON
* **REST API:** هو أسلوب معماري لتنظيم التواصل بين الأنظمة عبر بروتوكول الويب HTTP بشكل مستقل وغير مرتبط بالحالة (Stateless).
* **JSON:** هي صيغة خفيفة وسريعة لتمثيل وتبادل البيانات، وتعتمد على المفاتيح والقيم (`{"key": "value"}`) والمصفوفات (`[...]`)، وتعتبر الصيغة الأساسية المستخدمة في منصات الويب الحديثة وإطار Frappe / ERPNext.

---

### 5. توثيق الاختبار العملي عبر DevTools
* **طلب GET:** تم تنفيذ طلب جلب بيانات، واستجاب الخادم برمز الحالة `200 OK` ونوع المحتوى `application/json` (مرفق في الصورة: `get.png`).
* **طلب POST:** تم تنفيذ طلب إرسال بيانات، وقام الخادم باستلام الحمولة والرد بنجاح برمز الحالة `200 OK` (مرفق في الصورة post.png)
